describe("Registra usuario", () => {
    test("Registrar usuario pela primeira vez sem erro", () => {
        expect(1).toBe(1);
    });
    test("Registrar o mesmo usuario duas vezes e receber um erro", () => {
        expect(2).toBe(2);
    });
})